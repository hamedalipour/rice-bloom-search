# npm configuration for Cloudflare Pages build
# This fixes the Rollup optional dependencies issue

# Allow npm to install optional dependencies properly
optional=true

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Don't use package-lock during build (let npm resolve fresh)
package-lock=false