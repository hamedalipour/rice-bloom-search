# 🧪 راهنمای تست فیلتر دسته‌بندی

## ✅ مشکل حل شد!

فیلتر دسته‌بندی در صفحه فروشگاه اکنون به درستی کار می‌کند.

---

## 🔍 مشکل قبلی

قبلاً فیلتر دسته‌بندی کار نمی‌کرد چون:
- یک mapping اشتباه بین slug های فارسی و category_id وجود داشت
- کد سعی می‌کرد category_id را به فارسی تبدیل کند (مثلاً 'هاشمی')
- در حالی که در پایگاه داده، category_id به صورت انگلیسی است (مثلاً 'hashemi')

---

## ✅ راه حل

کد اصلاح شد و حالا:
- مقایسه مستقیم بین `selectedCategory` و `category_id` انجام می‌شود
- دیگر mapping فارسی/انگلیسی وجود ندارد
- فیلتر دقیقاً با مقادیر پایگاه داده کار می‌کند

---

## 🧪 نحوه تست

### روش 1: تست در مرورگر (پیشنهادی)

1. **اجرای برنامه:**
   ```bash
   npm run dev
   ```

2. **باز کردن صفحه فروشگاه:**
   - به `http://localhost:8080/shop` بروید

3. **تست فیلترها:**
   - کلیک روی "همه محصولات" - باید همه محصولات نمایش داده شوند
   - کلیک روی "هاشمی" - فقط محصولات با دسته‌بندی هاشمی
   - کلیک روی "طارم" - فقط محصولات با دسته‌بندی طارم
   - کلیک روی "فجر" - فقط محصولات با دسته‌بندی فجر
   - کلیک روی "شیرودی" - فقط محصولات با دسته‌بندی شیرودی

4. **بررسی Console (F12):**
   باید لاگ‌های زیر را ببینید:
   ```
   Filtering products. Total: X Category: hashemi
   After category filter: Y products with category: hashemi
   Filtered products: Y
   ```

### روش 2: تست اتوماتیک

اسکریپت تست جامع برای بررسی عملکرد فیلتر:

```bash
node test_category_filter.js
```

این اسکریپت تست می‌کند:
- ✅ مقادیر category_id در پایگاه داده
- ✅ نوع داده category_id (باید TEXT باشد)
- ✅ فیلتر کردن برای هر دسته‌بندی
- ✅ لاجیک فیلتر صفحه Shop
- ✅ تطابق بین کد و پایگاه داده

**نتیجه مورد انتظار:**
```
🎉 All tests passed! Category filter is working correctly.
```

---

## 📊 مقادیر معتبر category_id

در پایگاه داده، این مقادیر استفاده می‌شوند:

| نام فارسی | category_id (DB) | slug (URL) |
|-----------|------------------|------------|
| هاشمی     | `hashemi`        | `hashemi`  |
| طارم      | `tarom`          | `tarom`    |
| فجر       | `fajr`           | `fajr`     |
| شیرودی    | `shirodi`        | `shirodi`  |

**نکته**: همه به صورت lowercase انگلیسی هستند!

---

## 🔧 اگر هنوز کار نمی‌کند

### چک‌لیست عیب‌یابی:

1. **Cache مرورگر را پاک کنید:**
   ```
   Ctrl + Shift + R (Windows/Linux)
   Cmd + Shift + R (Mac)
   ```

2. **بررسی category_id محصولات:**
   ```sql
   SELECT id, name, category_id FROM products;
   ```
   
   همه category_id ها باید یکی از این‌ها باشند:
   - `hashemi`
   - `tarom`
   - `fajr`
   - `shirodi`
   - `null`

3. **اگر category_id ها اشتباه هستند:**
   
   برای تغییر یک محصول:
   ```sql
   UPDATE products 
   SET category_id = 'hashemi' 
   WHERE id = 'PRODUCT_ID_HERE';
   ```

4. **بررسی Console مرورگر (F12):**
   - باید لاگ‌های فیلتر را ببینید
   - اگر خطا دیدید، کپی کنید و بررسی کنید

5. **اجرای تست اتوماتیک:**
   ```bash
   node test_category_filter.js
   ```
   
   این تست به شما می‌گوید دقیقاً کجا مشکل است.

---

## 📝 نمونه استفاده در کد

```typescript
// در Shop.tsx - لاجیک فیلتر
const filteredProducts = useMemo(() => {
  let filtered = [...products];

  // Filter by category
  if (selectedCategory !== "all") {
    // مقایسه مستقیم - بدون mapping!
    filtered = filtered.filter((p) => p.category_id === selectedCategory);
  }

  return filtered;
}, [products, selectedCategory]);
```

---

## ✨ ویژگی‌های اضافی

فیلتر دسته‌بندی همراه با:

- ✅ **فیلتر قیمت** - slider برای انتخاب محدوده قیمت
- ✅ **مرتب‌سازی** - پیش‌فرض، محبوب‌ترین، بیشترین امتیاز، ارزان‌ترین، گران‌ترین
- ✅ **URL Sync** - دسته‌بندی در URL ذخیره می‌شود (`?category=hashemi`)
- ✅ **تعداد نتایج** - نمایش تعداد محصولات فیلتر شده
- ✅ **Empty State** - پیام مناسب هنگام عدم وجود محصول

---

## 🎯 نتایج مورد انتظار

### برای دسته‌بندی "هاشمی":
- URL: `http://localhost:8080/shop?category=hashemi`
- فیلتر: فقط محصولات با `category_id = 'hashemi'`
- UI: دکمه "هاشمی" active (سبز)

### برای "همه محصولات":
- URL: `http://localhost:8080/shop`
- فیلتر: تمام محصولات
- UI: دکمه "همه محصولات" active

---

## 📚 مستندات مرتبط

- `src/pages/Shop.tsx` - کد صفحه فروشگاه
- `src/data/products.ts` - تعریف دسته‌بندی‌ها
- `test_category_filter.js` - اسکریپت تست
- `SUPABASE_FIXES_APPLIED.md` - تغییرات پایگاه داده

---

## 🆘 پشتیبانی

اگر بعد از این مراحل هنوز مشکل دارید:

1. اسکرین‌شات از Console (F12) بگیرید
2. نتیجه این query را بفرستید:
   ```sql
   SELECT id, name, category_id FROM products;
   ```
3. نتیجه `node test_category_filter.js` را بفرستید

---

## ✅ تایید نهایی

فیلتر دسته‌بندی کار می‌کند اگر:

- [ ] در صفحه فروشگاه، همه دکمه‌های دسته‌بندی نمایش داده می‌شوند
- [ ] کلیک روی هر دسته‌بندی، فقط محصولات آن دسته را نشان می‌دهد
- [ ] URL با دسته‌بندی انتخابی به‌روز می‌شود
- [ ] تعداد محصولات فیلتر شده صحیح است
- [ ] Console هیچ خطایی ندارد
- [ ] تست اتوماتیک موفق است

---

**وضعیت**: ✅ حل شده  
**تاریخ**: 20 دی 1403  
**نسخه**: 1.0.0

🎉 **فیلتر دسته‌بندی حالا کامل کار می‌کند!**