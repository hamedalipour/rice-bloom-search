# Cloudflare Pages Build Configuration
# This file specifies build settings for Cloudflare Pages

build:
  # Build command - use npm install instead of npm ci for optional deps
  command: npm install && npm run build
  
  # Output directory
  publish: dist
  
  # Environment variables for build
  environment:
    NODE_VERSION: "18"
    NPM_CONFIG_PRODUCTION: "false"
    
  # Install command override
  install_command: npm install